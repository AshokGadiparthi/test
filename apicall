SELECT
  FORMAT_TIMESTAMP('%Y-%m-%d', insert_ts) AS event_date,
  CONCAT(
    LPAD(CAST(4 * DIV(EXTRACT(HOUR FROM insert_ts), 4) AS STRING), 2, '0'),
    ':00 - ',
    LPAD(CAST(4 * DIV(EXTRACT(HOUR FROM insert_ts), 4) + 4 AS STRING), 2, '0'),
    ':00'
  ) AS four_hour_bucket,
  COUNT(*) AS record_count
FROM your_table
WHERE insert_ts IS NOT NULL
GROUP BY event_date, four_hour_bucket
ORDER BY event_date, four_hour_bucket;
