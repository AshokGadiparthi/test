Statement statement = Statement.of("SELECT GENERATED_UUID() AS new_uuid");
try (ResultSet resultSet = dbClient.singleUse().executeQuery(statement)) {
    if (resultSet.next()) {
        String spannerUuid = resultSet.getString("new_uuid");
    }
}
