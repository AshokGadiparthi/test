PCollection<String> windowedJsonLines = jsonLines.apply(
                Window.<String>into(FixedWindows.of(Duration.standardMinutes(1))) // Adjust the window size as needed
                        .triggering(AfterPane.elementCountAtLeast(500)) // Trigger when at least 500 elements are accumulated
                        .discardingFiredPanes());
