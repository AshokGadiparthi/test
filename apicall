String spannerInstant = "2024-01-01T00:00:00.000Z";

        // Parse the string to Instant
        Instant instant = Instant.parse(spannerInstant);

        // Convert to LocalDateTime in UTC, then format to BigQuery DATETIME format
        LocalDateTime localDateTime = LocalDateTime.ofInstant(instant, ZoneOffset.UTC);
        String bigQueryDatetime = localDateTime.format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));

        System.out.println("BigQuery DATETIME format: " + bigQueryDatetime);
