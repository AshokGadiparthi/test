String keyType = jedis.type(key);
    if (!"string".equals(keyType) && jedis.exists(key)) {
        System.out.println("🚨 Deleting Invalid Key [" + key + "] of Type [" + keyType + "]");
        jedis.del(key); // ❌ Delete incorrect key type
    }
