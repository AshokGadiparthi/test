UPDATE your_table t
SET latest_flag = 'N'
WHERE STRUCT(t.customer_id, t.insert_ts) NOT IN (
  SELECT AS STRUCT customer_id, MAX(insert_ts)
  FROM your_table
  GROUP BY customer_id
  HAVING COUNT(*) > 1
);
