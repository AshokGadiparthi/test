CREATE OR REPLACE TABLE `your_project.your_dataset.your_temp_table_deduped` AS
SELECT cust_id, acct_num, mtn, 
       ARRAY_AGG(UUID LIMIT 1)[OFFSET(0)] AS UUID  -- ✅ Picks one UUID randomly
FROM `your_project.your_dataset.table1_temp`
GROUP BY cust_id, acct_num, mtn;
