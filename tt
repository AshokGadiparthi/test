if (!responses.isEmpty() && responses.get(0) != null) {
                    String value = (String) responses.get(0);
                    context.output(value);
                } else {
                    System.out.println("Key " + key + " not found in Redis.");
                }
